#include<cstdio>
#include<cstring>
#include<cstdlib>
#include<iostream>
#include<algorithm>
using namespace std;

const int N = 20000;

char s[N];
int n;

int main() {
	scanf("%d", &n);
	scanf("%s", s);
	int mask = (1 << n) - 1;
	for(int i = 0; i < (1 << n); i ++)
		if (
	return 0;
}
